# Normalize line endings for cross-platform consistency
* text=auto

# Python / config / docs → always LF
*.py    text eol=lf
*.yaml  text eol=lf
*.md    text eol=lf
*.txt   text eol=lf
*.sh    text eol=lf
*.toml  text eol=lf

# Windows batch files → always CRLF
*.bat   text eol=crlf
